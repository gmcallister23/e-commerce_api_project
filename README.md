This is a database that feature RESTful API structure and CRUD functionality

The data base feature model tables that organize data that has been add through API routes

The data is stored a MYSQL workbench

The project was written in Python utilizing Flask, SQLAlchemy and Marshmallow
